{% autoescape off %}

.well.well-small
  h4.boxTitle.well Exams

  if(exams.length == 0)
    .alert.alert-warning You don't have any exams!
      a.close(data-dismiss='alert', href='#') &times;

  for exam in exams
    .well.well-small=exam

{% endautoescape %}