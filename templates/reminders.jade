{% autoescape off %}

.well.well-small.box#reminderList
  h4.boxTitle.well.reminderHead
    i.icon.icon-bell
    |  Tasks and Reminders

    .btn-group.pull-right.addBtn(data-intro="Add and Sort Reminders", data-position="left") 
      a.btn.btn-small.btn-success(href="#addReminder")
        i.icon-plus.icon-white
      a.btn.btn-small.dropdown-toggle(data-toggle='dropdown')
        i.icon-list
      ul.dropdown-menu
        li: a.sort(data-key="0") Due Date 
          //-i.icon-check
        li: a.sort(data-key="1") Course
        li: a.sort(data-key="2") Type

  .alert.alert-warning.hide
    | Look at your syllabus! Add upcoming exams, projects, and homeworks.

  .list 
    .center ...loading

  .reminderBtns

    a.btn.btn-small.btn-warning.pull-right.showMore.hide
      i.icon-eye-open.icon-white
      | 
      span.msg Show More
    a.btn.btn-small.pull-right.viewAll.hide
      |  All Courses
 {% endautoescape %}